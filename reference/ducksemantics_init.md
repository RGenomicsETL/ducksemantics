# Initialize semantic graph tables

Initialize semantic graph tables

## Usage

``` r
ducksemantics_init(conn, prefix = "semantic")
```

## Arguments

- conn:

  DBI connection.

- prefix:

  Table prefix.

## Value

Invisibly, `conn`.
