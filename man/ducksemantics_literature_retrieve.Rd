% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relations.R
\name{ducksemantics_literature_retrieve}
\alias{ducksemantics_literature_retrieve}
\title{Retrieve exact literature spans from append-only caller-owned relations}
\usage{
ducksemantics_literature_retrieve(
  articles,
  sections,
  snapshots,
  query,
  provider_id,
  snapshot_id,
  section_names = NULL,
  conn = NULL
)
}
\arguments{
\item{articles}{Caller-owned relation with \code{provider_id}, \code{article_id},
\code{pmid}, \code{version_id}, finite integer \code{source_ordinal}, and logical
\code{is_deleted}.}

\item{sections}{Caller-owned relation with \code{provider_id}, \code{article_id},
\code{pmid}, \code{version_id}, finite integer \code{source_ordinal}, \code{section}, and
exact non-empty \code{text}.}

\item{snapshots}{Source snapshot catalog from
\code{\link[=ducksemantics_literature_snapshot_catalog]{ducksemantics_literature_snapshot_catalog()}}.}

\item{query}{Non-empty lexical query text.}

\item{provider_id}{Provider identity.}

\item{snapshot_id}{Exact provider snapshot identity.}

\item{section_names}{Optional non-empty section names to search; \code{NULL}
searches every version-bound section.}

\item{conn}{Optional DBI connection for table relations.}
}
\value{
Exact non-empty source spans from non-deleted article versions visible
at the cataloged source-order cutoff.
}
\description{
\code{RClinVarbitration} owns the article and section relations. Retrieval binds a
provider and exact cataloged source-order cutoff, selects the maximum article
\code{source_ordinal} at or below that cutoff per provider/article, drops a latest
deletion event, and joins only sections from that exact version. It never
imports, writes, or fetches literature.
}
\details{
A read-only RClinVarbitration projection joins \code{pubmed_sources} to
\code{pubmed_articles} and \code{pubmed_abstracts}: use source provider as
\code{provider_id}, source id as \code{version_id}/\code{snapshot_id}, source ordinal for
both ordinal fields, and \code{source_applied_at} as \code{effective_at}. A section
must retain the exact article event's version id and source ordinal.
}
