% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relations.R
\name{ducksemantics_monarch_project_pack}
\alias{ducksemantics_monarch_project_pack}
\title{Project one attached, dated Monarch annotation pack}
\usage{
ducksemantics_monarch_project_pack(
  conn,
  source_catalog,
  releases,
  provider_id,
  release_id,
  prefix = "semantic_monarch"
)
}
\arguments{
\item{conn}{Valid DBI connection with the pack already attached.}

\item{source_catalog}{Attached DuckDB catalog name, not the current catalog.}

\item{releases}{Typed Monarch release catalog or caller-owned catalog table.
This binds provider/release semantics; the caller must verify the immutable
file receipt before attaching the pack.}

\item{provider_id}{Exact provider identity in \code{releases}.}

\item{release_id}{Exact, dated release identity in \code{releases}; \code{"latest"} is
rejected.}

\item{prefix}{Safe unqualified prefix for temporary view names.}
}
\value{
A data frame naming the seven connection-local temporary views, with
\code{relation}, \code{view_name}, \code{provider_id}, and \code{release_id} columns. It does
not return pack contents.
}
\description{
Projects an already attached read-only official Monarch DuckDB pack into
connection-local temporary views. It never attaches, downloads, collects,
copies, rewrites, or merges pack rows. \code{source_catalog} must name an attached
DuckDB catalog whose \code{main}
schema contains the official \code{denormalized_edges}, \code{nodes},
\code{information_content}, and \code{node_has_phenotype} tables. The edge table is
checked before any view is made, including its scalar role categories,
provider-serialized text negation, and array provenance fields. The official
required \code{nodes}, \code{node_has_phenotype}, and \code{information_content} projection
columns/types are also validated.
}
\details{
\code{associations} retains every source edge and all source columns exactly, then
adds the selected provider and release. Each role view is likewise a total
projection: it retains raw source columns and adds normalized role columns,
\code{source_subject}, \code{source_object}, \code{source_direction}, and
\code{association_status}. Only \code{supported_subject_to_object} and
\code{supported_object_to_subject} rows have non-missing normalized roles.
\code{missing_endpoint}, \code{missing_predicate}, \code{malformed_negation},
\code{missing_role_category}, \code{malformed_role_category}, \code{negated}, and
\code{unsupported_orientation} are not support. \code{negation_status} separately
distinguishes \code{not_provided}, \code{not_negated}, \code{negated}, and \code{malformed}:
Monarch omits the optional qualifier on many positive assertion rows, so
omission is retained but does not erase the assertion. In particular, a
missing predicate never becomes a causal predicate.
}
